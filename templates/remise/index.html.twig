

{% extends 'base.html.twig' %}
{% block body %}

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
<div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 150px;">


    <table class="table table-striped" border="1">
    <thead>
    
   <th>  libelle </th>
    
      <th>  ancienPrix </th>
    
       <th>  nouveauPrix </th>
      
      <th>  Suppression </th>
      <th>  Modification </th>
      
    </thead>
    <tbody>
    {% for remise in r %}
    <tr>
    
    <td>{{remise.libelle}}</td>
   
    <td>{{remise.AncienPrix}}</td>
    
    <td>{{remise.nouveauPrix}}</td>
    
   

    <td> <a class="btn btn-success" href="{{ path('supp_remise',{'id':remise.id} ) }}">Supprimer</a></td>
    <td> <a  class="btn btn-danger" href="{{ path('modif_rm',{'id':remise.id} ) }}">Modifier</a></td>
    <td><a class="btn btn-success" href="{{ path('addRemise') }}">Ajouter</a></td>
   </tr>
  {% endfor %}
   </tbody>
   </table>
   </div>
{% endblock %}
