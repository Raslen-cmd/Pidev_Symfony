

{% extends 'base.html.twig' %}
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
<div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 150px;">


     <table class="table table-striped" border="1">
    <thead>
    <th> id</th>
   <th>  numcom </th>
    {# <th> dateComm </th> #}
      <th>  observation </th>
       {#<th>  id_client </th>#}
       <th>  tht </th>
      <th> ttva </th>
      <th>  tttc </th>
     
      
      <th>  Suppression </th>
      <th>  Modification </th>
      <th>  Ajouter </th>
       
      
    </thead>
    <tbody>
    {% for commande in c %}
    <tr>
    <td>{{commande.id}}</td>
    <td>{{commande.numcom}}</td>
   {# <td>{{commande.dateComm}}</td> #}
    <td>{{commande.observation}}</td>
    
    <td>{{commande.tht}}</td>
    
    <td>{{commande.ttva}}</td>
    <td>{{commande.tttc}}</td>

     
     
     
    <td> <a class="btn btn-success" href="{{ path('supp_commande',{'id':commande.id} ) }}">Supprimer</a></td>
    <td> <a  class="btn btn-danger" href="{{ path('modif_cmd',{'id':commande.id} ) }}">Modifier</a></td>
    

    <td><a class="btn btn-success" href="{{ path('addCommande') }}">Ajouter</a></td>
   </tr>
  {% endfor %}
   </tbody>
   </table>
   </div>
{% endblock %}
